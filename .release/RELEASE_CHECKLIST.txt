* Check version changed in file VERSION
* Check README.md genereated from template (replace version placeholders)
* Check annotated tag on commit with version (git tag -s -a "v$(cat VERSION)" -m "v$(cat VERSION)")